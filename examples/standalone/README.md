# HTMDoc Standalone Example

This example exercises full HTML, fragment, template and Custom Elements
Manifest inputs with one JSON config.

From the repository root:

```sh
npm run smoke:standalone
```

The command writes extraction artifacts, HIA documents, direct-source
documentation maps and `htmdoc.producer-result.json` under `dist/htmdoc`.
Source content is not embedded by default.
